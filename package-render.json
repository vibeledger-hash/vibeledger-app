{
  "name": "microshield-payments",
  "version": "1.0.0",
  "description": "MicroShield Payments - Secure mobile payment system",
  "main": "backend/standalone-server.js",
  "scripts": {
    "start": "cd backend && npm install && node standalone-server.js",
    "build": "cd backend && npm install",
    "dev": "cd backend && npm run dev",
    "postinstall": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18"
  }
}
